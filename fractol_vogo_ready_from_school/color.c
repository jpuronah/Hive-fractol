
# define A 0x201923
# define B 0xffffff
# define C 0xfcff5d
# define D 0x7dfc00
# define E 0x0ec434
# define F 0x228c68
# define G 0x8ad8e8
# define H 0x235b54
# define I 0x29bdab
# define J 0x3998f5
# define K 0x37294f
# define L 0x277da7
# define M 0x3750db
# define N 0xf22020
# define O 0x991919
# define P 0xffcba5
# define Q 0xe68f66
# define R 0xc56133
# define S 0x96341c
# define T 0x632819
# define U 0xffc413
# define V 0xf47a22
# define W 0x2f2aa0
# define X 0xb732cc
# define Y 0x772b9d
# define Z 0xf07cab
# define AA 0xd30b94
# define AB 0xedeff3
# define AC 0xc3a5b4
# define AD 0x946aa2
# define AE 0x5d4c86


/*static int	colour(int n)
{
	if (n >= 25)
		return (LEMONCHIFFON);
	if (n < 5)
		return (HOTPINK);
	if (n < 7)
		return (FIREBRICK);
	if (n < 9)
		return (BLUE);
	if (n < 2)
		return (GRAY);
	if (n < 11)
		return (GREEN);
	if (n < 13)
		return (PURPLE);
	if (n < 15)
		return (RED);
	if (n < 17)
		return (ALICEBLUE);
	if (n < 19)
		return (CORAL);
	if (n < 21)
		return (WHITE);
	else
		return (BLACK);
	return (0);
}*/